# Markdown Translator 开发指南

## 项目概述

这是一个功能完整的VSCode扩展插件，专门用于markdown文档的智能翻译。项目采用TypeScript开发，支持多种翻译引擎。

## 项目结构

```
markdown-translator/
├── src/                          # 源代码目录
│   ├── extension.ts              # 主入口文件
│   ├── services/                 # 服务层
│   │   └── translatorService.ts  # 翻译服务管理器
│   ├── engines/                  # 翻译引擎
│   │   ├── googleTranslator.ts   # Google翻译
│   │   ├── baiduTranslator.ts    # 百度翻译
│   │   ├── openaiTranslator.ts   # OpenAI翻译
│   │   └── claudeTranslator.ts   # Claude翻译
│   └── utils/                    # 工具类
│       └── markdownProcessor.ts  # Markdown处理器
├── out/                          # 编译输出目录
├── .vscode/                      # VSCode配置
├── package.json                  # 扩展清单
├── tsconfig.json                 # TypeScript配置
├── README.md                     # 使用说明
├── CHANGELOG.md                  # 版本历史
└── test-sample.md               # 测试示例
```

## 开发环境设置

### 1. 安装依赖
```bash
npm install
```

### 2. 编译项目
```bash
npm run compile
```

### 3. 监视模式（开发时）
```bash
npm run watch
```

### 4. 代码检查
```bash
npm run lint
```

## 调试和测试

### 1. 启动调试
1. 在VSCode中打开项目
2. 按F5或使用"Run Extension"配置
3. 会打开一个新的VSCode窗口，插件已加载

### 2. 测试功能
1. 在新窗口中打开`test-sample.md`文件
2. 尝试各种翻译命令：
   - `Ctrl+Shift+P` → "翻译为英文"
   - `Ctrl+Shift+P` → "翻译为中文"
   - 选中文本后右键 → "翻译选中文本"

### 3. 配置测试
在设置中搜索"Markdown Translator"，配置不同的翻译引擎API密钥进行测试。

## 核心架构

### 翻译服务架构
```
TranslatorService
    ├── GoogleTranslator    (免费，无需配置)
    ├── BaiduTranslator     (需要APP ID和密钥)
    ├── OpenAITranslator    (需要API密钥)
    └── ClaudeTranslator    (需要API密钥)
```

### 处理流程
1. **内容提取**: MarkdownProcessor提取可翻译内容
2. **智能过滤**: 跳过代码块、链接等不需翻译的部分
3. **批量翻译**: TranslatorService处理翻译请求
4. **内容重组**: 将翻译结果重新组装为完整文档

## 扩展功能

### 添加新的翻译引擎
1. 在`src/engines/`下创建新的翻译器类
2. 实现`ITranslator`接口
3. 在`TranslatorService`中注册新引擎

```typescript
// 示例：新翻译引擎
export class NewTranslator implements ITranslator {
    public isConfigured(): boolean {
        // 检查配置
    }
    
    public async translate(text: string, targetLanguage: string): Promise<string> {
        // 实现翻译逻辑
    }
}
```

### 添加新的命令
1. 在`package.json`的`contributes.commands`中声明
2. 在`extension.ts`的`registerCommands`中实现

## 打包发布

### 1. 安装VSCE
```bash
npm install -g @vscode/vsce
```

### 2. 打包扩展
```bash
npm run package
```

### 3. 发布到市场
```bash
vsce publish
```

## 配置选项

| 配置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| defaultEngine | string | "google" | 默认翻译引擎 |
| autoFilter | boolean | true | 自动过滤代码块 |
| autoTranslateOnOpen | boolean | false | 打开时自动翻译 |
| openaiApiKey | string | "" | OpenAI API密钥 |
| claudeApiKey | string | "" | Claude API密钥 |
| baiduAppId | string | "" | 百度翻译APP ID |
| baiduSecretKey | string | "" | 百度翻译密钥 |

## 故障排除

### 常见问题

1. **编译错误**
   - 确保所有依赖已安装：`npm install`
   - 检查TypeScript版本兼容性

2. **翻译失败**
   - 检查网络连接
   - 验证API密钥配置
   - 查看VSCode开发者控制台错误信息

3. **插件不加载**
   - 检查`package.json`配置
   - 确保编译成功，`out`目录存在

## 贡献指南

1. Fork项目
2. 创建功能分支
3. 提交更改
4. 创建Pull Request

## 技术栈

- **TypeScript**: 主要开发语言
- **VSCode Extension API**: 扩展开发框架
- **Axios**: HTTP请求库
- **crypto-js**: 加密库（百度翻译签名）

## 性能优化

- 添加请求延迟避免API频率限制
- 实现错误重试机制
- 缓存翻译结果（可选功能）
- 异步批量处理大文档
